// Practise with AND(&&)
0 && false; // 0
0 && true; // 0
true && NaN; // NaN
true && !1; // false
!0 && ""; // ""

//Practise OR(||)
0 || false; // false
true || false; // true
true || 1; // true
!true || !false; // true